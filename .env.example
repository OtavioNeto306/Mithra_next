# ========================================
# CONFIGURAÇÕES OBRIGATÓRIAS - ORACLE/ODBC
# ========================================
# Configurações do Banco de Dados Oracle/ODBC
ODBC_DSN=                              # Nome do DSN configurado (OBRIGATÓRIO)
ORACLE_USER=                           # Usuário do Oracle (OBRIGATÓRIO)
ORACLE_PASSWORD=                       # Senha do Oracle (OBRIGATÓRIO)
ORACLE_CONNECTION_STRING=              # String de conexão (OBRIGATÓRIO)

# Pool de conexões Oracle (opcional)
ORACLE_POOL_MIN=2                      # Mínimo de conexões no pool
ORACLE_POOL_MAX=5                      # Máximo de conexões no pool
ORACLE_POOL_INCREMENT=1                # Incremento do pool

# ========================================
# CONFIGURAÇÕES OPCIONAIS - MYSQL
# ========================================
# Configurações do Banco de Dados MySQL
MYSQL_HOST=localhost                   # Host do MySQL
MYSQL_PORT=3306                        # Porta do MySQL
MYSQL_USER=                            # Usuário do MySQL
MYSQL_PASSWORD=                        # Senha do MySQL
MYSQL_DATABASE=                        # Nome do banco de dados

# Configurações de pool MySQL
MYSQL_CONNECTION_LIMIT=50              # Limite de conexões simultâneas
MYSQL_QUEUE_LIMIT=100                  # Limite da fila de conexões
MYSQL_ACQUIRE_TIMEOUT=60000            # Timeout para adquirir conexão (ms)
MYSQL_TIMEOUT=60000                    # Timeout de query (ms)
MYSQL_IDLE_TIMEOUT=300000              # Timeout para conexões ociosas (ms)
MYSQL_SSL_ENABLED=false                # Habilitar SSL para MySQL (true/false)

# ========================================
# CONFIGURAÇÕES DO AMBIENTE
# ========================================
NODE_ENV=development                   # Ambiente (development/production)
NEXT_PUBLIC_APP_URL=http://localhost:3000  # URL pública da aplicação

# ========================================
# CONFIGURAÇÕES DO MAPA
# ========================================
# Mapa de Trajetória
# O projeto agora utiliza Leaflet.js com OpenStreetMap (gratuito)
# Não são necessárias chaves de API ou configurações de billing
# 
# Leaflet.js é uma biblioteca JavaScript open-source para mapas interativos
# OpenStreetMap fornece dados de mapa gratuitos e de código aberto
# Documentação: https://leafletjs.com/

# ========================================
# CONFIGURAÇÕES DE SEGURANÇA
# ========================================
JWT_SECRET=                            # Chave secreta para JWT
ENCRYPTION_KEY=                        # Chave para criptografia

# ========================================
# CONFIGURAÇÕES DE LOG
# ========================================
LOG_LEVEL=info                         # Nível de log (debug/info/warn/error)
LOG_FILE_PATH=./logs                   # Caminho para arquivos de log

# ========================================
# INSTRUÇÕES DE CONFIGURAÇÃO
# ========================================
# 1. Copie este arquivo para .env
# 2. Preencha todas as variáveis obrigatórias
# 3. O mapa de trajetória agora utiliza Leaflet.js (OpenStreetMap) - não requer configuração adicional
# 5. Configure restrições de HTTP referrers para segurança
# 6. Nunca commite o arquivo .env com valores reais